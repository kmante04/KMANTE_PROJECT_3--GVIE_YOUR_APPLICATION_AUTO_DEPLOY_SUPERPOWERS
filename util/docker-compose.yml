# docker-compose.yml
version: "3"
services:
  database:
    image: "postgres"
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=mante270123
      - POSTGRES_DB=udapeople-prod
    ports:
      - "5532:5432"
